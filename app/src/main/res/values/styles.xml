<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="EventActionButton">
        <item name="android:layout_width">@dimen/action_button_image_size</item>
        <item name="android:layout_height">@dimen/action_button_image_size</item>
        <item name="android:background">@drawable/background_white_circle</item>
        <item name="android:padding">@dimen/margin_card_medium_2</item>
        <item name="android:elevation">@dimen/margin_small</item>
    </style>

    <style name="TransparantStatusBar" parent="AppTheme.NoActionBar">
        <item name="android:windowTranslucentStatus">true</item>
    </style>

</resources>
